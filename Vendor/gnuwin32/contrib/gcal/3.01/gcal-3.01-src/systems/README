README file for special targets.

For MS-DOS, OS/2, Windows NT, VMS, Acorn, Atari, Amiga, Primos, use the
Makefile(s) or command file(s) provided in the appropriate subdirectory
and edit the `LIB', `INCLUDE', `[]FLAGS' and `CC' entries (if
necessary) to reflect your local system.

Then copy the modified file to the top-level `src/' directory and run
it.

Inspect the top-level `README' and `INSTALL' files for more information.

